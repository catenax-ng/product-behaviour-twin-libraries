---
credentials:
  cloud:
    accessKey: &cloudKey zuf+tfteSlswRu7BJ86wekitnifILbZam1KYY3TG
    secretKey: &cloudSecret someSecret
  apiwrapper:
    username: &apiwrapperUser user
    password: &apiwrapperPass pass
  api:
    username: &apiUser user
    password: &apiPass pass
  rawdata:
    username: &rawdataUser rawdata_user
    password: &rawdataPass rawdata_pass
  healthindicator:
    username: &hiUser healthindicator_user
    password: &hiPass healthindicator_pass

oem-hi-app:
  ingress:
    enabled: true
    className: ""
    tls: true
    certManager:
      issuer: ""
      clusterIssuer: ""

    annotations: {}
    hostname: "bmw-hiapp.dev.demo.catena-x.net"
    secret: tls-secret

  server:
    port: 25553

  connection:
    endpoints:
      default:
        port: 25553
        path: /
        ingress: True

  credentials:
    cloud:
      accessKey: *cloudKey
      secretKey: *cloudSecret
    apiwrapper:
      username: *apiwrapperUser
      password: *apiwrapperPass
    api:
      username: *apiUser
      password: *apiPass
    rawdata:
      username: *rawdataUser
      password: *rawdataPass
    healthindicator:
      username: *hiUser
      password: *hiPass

  configuration:
    properties: |-
      spring.application.name=hibackendservice
      spring.jpa.open-in-view=false
      edc.apiwrapper.submodelPath=api/service
      edc.apiwrapper.submodel=submodel
      edc.apiwrapper.providerEdcUrlKey=provider-connector-url
      server.port=25553
      cloud.endpoint=https://play.min.io
      cloud.region=eu-west-1
      cloud.bucket=test
      cloud.inputFile.bucketName=testbucket
      cloud.inputFile.key=testfile
      supplier.hiservice.endpoint=http://edc.zf.com/endpoint
      supplier.bpn=BPN0000SUPPLIER
      supplier.hiservice.inputAssetName=urn:example:id
      edc.bpn=BPN0000OEM
      edc.endpoint=https://oem.com/edc
      edc.apiwrapper.url=http://myapiwrapper.url
      security.api.role=USER
      rawdatadb.drivername=org.postgresql.Driver
      rawdatadb.show-sql=false
      rawdatadb.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
      rawdatadb.hibernate.hbm2ddl.auto=update
      rawdatadb.drivername=org.postgresql.Driver
      rawdatadb.show-sql=false
      rawdatadb.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
      rawdatadb.hibernate.hbm2ddl.auto=update
      rawdatadb.url=jdbc:postgresql://rawdatadb/rawdata
      healthindicatordb.url=jdbc:postgresql://healthindicatordb/healthindicator
      spring.config.import=

provider:
  ingress:
    enabled: true
    className: ""
    tls: true
    certManager:
      issuer: ""
      clusterIssuer: ""

    annotations: {}
    hostname: "bmw-provider.dev.demo.catena-x.net"
    secret: tls-secret

  server:
    port: 25553

  connection:
    endpoints:
      default:
        port: 25553
        path: /
        ingress: True

  credentials:
    cloud:
      accessKey: *cloudKey
      secretKey: *cloudSecret
    apiwrapper:
      username: *apiwrapperUser
      password: *apiwrapperPass
    api:
      username: *apiUser
      password: *apiPass
    rawdata:
      username: *rawdataUser
      password: *rawdataPass
    healthindicator:
      username: *hiUser
      password: *hiPass

  configuration:
    properties: |-
      spring.application.name=hibackendservice
      spring.jpa.open-in-view=false
      edc.apiwrapper.submodelPath=api/service
      edc.apiwrapper.submodel=submodel
      edc.apiwrapper.providerEdcUrlKey=provider-connector-url
      server.port=25553
      cloud.endpoint=https://play.min.io
      cloud.region=eu-west-1
      cloud.bucket=test
      cloud.inputFile.bucketName=testbucket
      cloud.inputFile.key=testfile
      supplier.hiservice.endpoint=http://edc.zf.com/endpoint
      supplier.bpn=BPN0000SUPPLIER
      supplier.hiservice.inputAssetName=urn:example:id
      edc.bpn=BPN0000OEM
      edc.endpoint=https://oem.com/edc
      edc.apiwrapper.url=http://myapiwrapper.url
      security.api.role=USER
      rawdatadb.drivername=org.postgresql.Driver
      rawdatadb.show-sql=false
      rawdatadb.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
      rawdatadb.hibernate.hbm2ddl.auto=update
      rawdatadb.drivername=org.postgresql.Driver
      rawdatadb.show-sql=false
      rawdatadb.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
      rawdatadb.hibernate.hbm2ddl.auto=update
      spring.config.import=healthindicatordb-bindings.properties
      rawdatadb.url=jdbc:postgresql://rawdatadb/rawdata
      healthindicatordb.url=jdbc:postgresql://healthindicatordb/healthindicator
      spring.config.import=
      server.port=25551
      services.dataupdater.endpoint.baseurl=http://bmw-updater.dev.demo.catena-x.net/api/rawdata
      services.dataprovider.testdata.file=testdata.json
      services.dataupdater.endpoint.username=user
      services.dataupdater.endpoint.password=pass
      services.dataupdater.endpoint.authenticationActivated=true
      security.api.username=user
      security.api.password=pass
      security.api.role=USER

updater:
  ingress:
    enabled: true
    className: ""
    tls: true
    certManager:
      issuer: ""
      clusterIssuer: ""

    annotations: {}
    hostname: "bmw-updater.dev.demo.catena-x.net"
    secret: tls-secret

  server:
    port: 25553

  connection:
    endpoints:
      default:
        port: 25553
        path: /
        ingress: True

  credentials:
    cloud:
      accessKey: *cloudKey
      secretKey: *cloudSecret
    apiwrapper:
      username: *apiwrapperUser
      password: *apiwrapperPass
    api:
      username: *apiUser
      password: *apiPass
    rawdata:
      username: *rawdataUser
      password: *rawdataPass
    healthindicator:
      username: *hiUser
      password: *hiPass

  configuration:
    properties: |-
      spring.application.name=hibackendservice
      spring.jpa.open-in-view=false
      edc.apiwrapper.submodelPath=api/service
      edc.apiwrapper.submodel=submodel
      edc.apiwrapper.providerEdcUrlKey=provider-connector-url
      server.port=25553
      cloud.endpoint=https://play.min.io
      cloud.region=eu-west-1
      cloud.bucket=test
      cloud.inputFile.bucketName=testbucket
      cloud.inputFile.key=testfile
      supplier.hiservice.endpoint=http://edc.zf.com/endpoint
      supplier.bpn=BPN0000SUPPLIER
      supplier.hiservice.inputAssetName=urn:example:id
      edc.bpn=BPN0000OEM
      edc.endpoint=https://oem.com/edc
      edc.apiwrapper.url=http://myapiwrapper.url
      security.api.role=USER
      rawdatadb.drivername=org.postgresql.Driver
      rawdatadb.show-sql=false
      rawdatadb.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
      rawdatadb.hibernate.hbm2ddl.auto=update
      rawdatadb.drivername=org.postgresql.Driver
      rawdatadb.show-sql=false
      rawdatadb.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
      rawdatadb.hibernate.hbm2ddl.auto=update
      spring.config.import=healthindicatordb-bindings.properties
      rawdatadb.url=jdbc:postgresql://rawdatadb/rawdata
      healthindicatordb.url=jdbc:postgresql://healthindicatordb/healthindicator
      spring.config.import=


rawdatadb:
  fullnameOverride: "rawdatadb"
  image:
    pullPolicy: Always
  auth:
    database: &psqlDatabase "rawdata"
    username: *rawdataUser
    password: *rawdataPass

  persistence:
    enabled: false

  # initdbUsername: *psqlUsername
  # initdbPassword: *psqlPassword

healthindicatordb:
  fullnameOverride: "healthindicatordb"
  image:
    pullPolicy: Always
  auth:
    database: &psqlDatabase "healthindicator"
    username: *hiUser
    password: *hiPass

  persistence:
    enabled: false

  # initdbUsername: *psqlUsername
  # initdbPassword: *psqlPassword